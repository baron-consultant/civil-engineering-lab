# Analytics (Aptabase)
# In CI these come from GitHub Actions Variables.
# PUBLIC_APTABASE_URL can be either the base host (https://us.aptabase.com) or a full endpoint (https://us.aptabase.com/api/v0/event).
PUBLIC_APTABASE_URL=https://us.aptabase.com
PUBLIC_APTABASE_KEY=A-US-XXXXX
PUBLIC_APTABASE_DEBUG=false
PUBLIC_COMMIT_SHA=local

# Playwright (e2e)
# Base URL used by tests when you run a preview server, e.g. `npm run dev -- --host --port 4173`
PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173
